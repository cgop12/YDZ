# TypeBattle 需求规格说明书 (SRS)

| 文档信息 | |
|---------|---|
| **项目名称** | TypeBattle（打字对战） |
| **文档版本** | v1.0 |
| **创建日期** | 2026-05-12 |
| **文档状态** | 正式版 |
| **最后更新** | 2026-05-12 |

---

## 一、项目概述

### 1.1 项目背景

TypeBattle 是一款局域网多人实时打字对战游戏，旨在通过趣味竞技方式提升用户的打字速度和准确率。项目采用 Client-Server 架构，支持 2-32 名玩家在同一局域网内进行淘汰赛制打字比赛。

### 1.2 项目目标

| 目标类型 | 描述 |
|---------|------|
| **核心目标** | 提供流畅、稳定的局域网打字对战体验 |
| **性能目标** | 网络延迟 < 100ms，UI 响应 < 16ms（60fps） |
| **兼容性目标** | 支持 Windows 10/11，需 PyQt6 运行库 |
| **可用性目标** | 无需配置，UDP 自动发现服务器 |

### 1.3 术语定义

| 术语 | 定义 |
|------|------|
| **Tournament** | 淘汰赛制比赛，包含多轮对决 |
| **Match** | 单场比赛，包含两名玩家 |
| **Round** | 比赛轮次（如 8 强、4 强、半决赛、决赛） |
| **Text** | 打字比赛用的文本内容 |
| **Admin** | 管理员，有权限开始/暂停/结束比赛 |
| **Walkover** | 轮空，当参赛人数为奇数时某人直接晋级 |

---

## 二、功能需求

### 2.1 功能列表总览

```
┌─────────────────────────────────────────────────────────────┐
│                      TypeBattle 功能矩阵                      │
├─────────────────────────────────────────────────────────────┤
│  [✓] 服务发现   [✓] 用户注册   [✓] 大厅界面   [✓] 准备状态    │
│  [✓] 淘汰赛制   [✓] 实时对战   [✓] 进度追踪   [✓] 胜负判定    │
│  [✓] 暂停/恢复  [✓] 断线重连   [✓] 心跳检测   [✓] 排行榜     │
│  [✓] 轮空处理   [✓] 全屏模式   [✓] 音效提示   [ ] AI 对战    │
│  [ ] 观战模式   [ ] 排行榜历史  [ ] 自定义题目 [ ] 计分规则   │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 服务发现与连接

#### FR-001: UDP 服务发现
| 属性 | 描述 |
|------|------|
| **优先级** | P0 (核心) |
| **功能描述** | 客户端通过 UDP 广播自动发现局域网内的游戏服务器 |
| **输入** | UDP 广播 `type_battle_discover` 到端口 23333 |
| **输出** | 服务器返回 `{"ip": "...", "port": 8888, "room": "...", "count": N, "max": 32}` |
| **超时处理** | 1.2 秒内未收到响应，提示手动输入 IP |
| **可选参数** | 支持 `?room=1234` 查询指定房间 |

#### FR-002: WebSocket 连接
| 属性 | 描述 |
|------|------|
| **优先级** | P0 (核心) |
| **协议** | WebSocket (ws://) |
| **端口** | 8888 (默认) |
| **消息格式** | JSON + `\n` (每行一条消息) |
| **编码** | UTF-8 |

#### FR-003: 手动 IP 连接
| 属性 | 描述 |
|------|------|
| **优先级** | P1 (重要) |
| **功能描述** | 当 UDP 发现失败时，允许用户手动输入服务器 IP |
| **输入验证** | 格式: `192.168.1.100` 或 `192.168.1.100:8888` |
| **错误处理** | 无效 IP 格式时提示重新输入 |

### 2.3 用户认证与大厅

#### FR-010: 加入房间
| 属性 | 描述 |
|------|------|
| **优先级** | P0 (核心) |
| **输入** | 玩家名称（必填），房间号（可选） |
| **名字规则** | 1-20 字符，唯一性校验 |
| **房间容量** | 最大 32 人 |
| **错误码** | `NAME_TAKEN`（名字已占用）、`ROOM_FULL`（房间已满） |

#### FR-011: 玩家列表
| 属性 | 描述 |
|------|------|
| **优先级** | P0 (核心) |
| **更新时机** | 玩家加入、离开、准备状态变化时 |
| **显示内容** | 玩家名称、准备状态、是否管理员 |

#### FR-012: 准备状态
| 属性 | 描述 |
|------|------|
| **优先级** | P1 (重要) |
| **功能描述** | 玩家点击准备按钮表示已就绪 |
| **自动准备** | 30 秒未操作自动准备 |
| **开始条件** | 所有玩家准备后，管理员可开始比赛 |
| **状态广播** | `ready_status` 消息，通知所有玩家准备情况 |

### 2.4 比赛流程

#### FR-020: 赛前倒计时
| 属性 | 描述 |
|------|------|
| **优先级** | P0 (核心) |
| **倒计时** | 5 秒（5-4-3-2-1） |
| **显示方式** | 全屏居中大字 |
| **消息类型** | `countdown` |

#### FR-021: 比赛开始
| 属性 | 描述 |
|------|------|
| **优先级** | P0 (核心) |
| **消息类型** | `match_begin` |
| **包含内容** | 轮次编号、轮次名称、对手名称、比赛文本、字数限制、时间限制 |
| **UI 要求** | 显示原文、对手名、启用输入框 |

#### FR-022: 逐字校验
| 属性 | 描述 |
|------|------|
| **优先级** | P0 (核心) |
| **客户端行为** | 每输入一个字符发送 `keystroke` 消息 |
| **服务端行为** | 校验正确性，返回 `keystroke_ack` |
| **正确** | 继续输入，下一字符高亮 |
| **错误** | 红色下划线标记，错误计数 +1 |
| **暂停处理** | 暂停期间忽略所有按键 |

#### FR-023: 实时时间同步
| 属性 | 描述 |
|------|------|
| **优先级** | P0 (核心) |
| **服务端广播** | 每 200ms 广播 `time_sync` |
| **客户端行为** | 直接使用服务端时间，不本地倒计时 |
| **暂停处理** | `paused=true` 时锁定显示 |

#### FR-024: 进度上报
| 属性 | 描述 |
|------|------|
| **优先级** | P1 (重要) |
| **上报频率** | 每 200ms |
| **内容** | 已完成字数、错误数、已用时间 |
| **暂停处理** | 暂停期间忽略 |

### 2.5 比赛控制

#### FR-030: 暂停比赛
| 属性 | 描述 |
|------|------|
| **优先级** | P1 (重要) |
| **权限** | 仅管理员可操作 |
| **消息类型** | `pause_request` |
| **服务端行为** | 设置 `paused=true`，记录当前时间 |
| **客户端行为** | 停止输入、显示"比赛暂停"覆盖层 |
| **错误码** | `NOT_ADMIN`、`ALREADY_PAUSED` |

#### FR-031: 恢复比赛
| 属性 | 描述 |
|------|------|
| **优先级** | P1 (重要) |
| **权限** | 仅管理员可操作 |
| **消息类型** | `resume_request` |
| **服务端行为** | 设置 `paused=false` |
| **客户端行为** | 恢复输入、隐藏覆盖层 |

#### FR-032: 强制结束
| 属性 | 描述 |
|------|------|
| **优先级** | P2 (一般) |
| **权限** | 仅管理员可操作 |
| **消息类型** | `admin_end` |
| **客户端行为** | 显示结束原因，返回大厅 |

### 2.6 比赛结束

#### FR-040: 轮次结束
| 属性 | 描述 |
|------|------|
| **优先级** | P0 (核心) |
| **胜负判定** | 服务端根据计分规则计算 |
| **轮空检测** | `winner=="" && score==100 && elapsed==0` |
| **消息类型** | `round_end` |
| **包含内容** | 胜利者姓名、双方比赛结果（完成率、正确率、用时、得分） |

#### FR-041: 最终排名
| 属性 | 描述 |
|------|------|
| **优先级** | P0 (核心) |
| **触发条件** | 淘汰赛全部结束 |
| **消息类型** | `final_ranking` |
| **排名规则** | 按最终得分降序排列 |
| **展示内容** | 排名、姓名、得分、完成率、准确率 |

### 2.7 计分系统

#### FR-050: 得分计算
| 属性 | 描述 |
|------|------|
| **优先级** | P0 (核心) |
| **计算公式** | 见下方 |
| **执行方** | 服务端 |

```
总得分 = 完成率 × 60% + 正确率 × 30% + 速度得分 × 10%

其中：
- 完成率 = (已完成字数 / 总字数) × 100
- 正确率 = ((已完成字数 - 错误数) / 已完成字数) × 100
- 速度得分 = min(100, (已完成字数 / 用时) / 3.0 × 100)
```

### 2.8 断线重连

#### FR-060: 断线检测
| 属性 | 描述 |
|------|------|
| **优先级** | P1 (重要) |
| **检测方式** | 心跳机制（5 秒未收到 pong 判定断线） |
| **心跳间隔** | 服务端每 2 秒发送 `ping` |
| **客户端响应** | 收到 `ping` 立即回复 `pong` |

#### FR-061: 状态保留
| 属性 | 描述 |
|------|------|
| **优先级** | P1 (重要) |
| **保留时间** | 10 秒 |
| **保留内容** | 玩家ID、当前位置、已完成字数、错误数 |

#### FR-062: 重连恢复
| 属性 | 描述 |
|------|------|
| **优先级** | P1 (重要) |
| **消息类型** | `reconnect` |
| **服务端响应** | `full_state_snapshot` |
| **失败处理** | `RECONNECT_FAILED`，返回大厅 |

### 2.9 客户端 UI

#### FR-070: 页面结构
| 页面 | 描述 |
|------|------|
| **登录页** | 服务器连接、名称输入 |
| **大厅页** | 玩家列表、准备按钮、开始按钮（管理员） |
| **比赛页** | 打字区域、计时器、对手进度 |
| **结果页** | 轮次结果、最终排名、返回大厅 |

#### FR-071: 快捷键
| 快捷键 | 功能 |
|--------|------|
| **F11** | 切换全屏/窗口 |
| **Esc** | 退出确认（全屏模式下） |

#### FR-072: 字体自适应
| 属性 | 描述 |
|------|------|
| **规则** | `fontSize = max(12, min(20, width/55))` |
| **触发时机** | `resizeEvent` 中重新计算 |

---

## 三、非功能需求

### 3.1 性能需求

| 指标 | 要求 | 说明 |
|------|------|------|
| **网络延迟** | < 100ms | 局域网内正常网络 |
| **UI 帧率** | ≥ 60fps | 16ms 内完成渲染 |
| **启动时间** | < 3 秒 | 从点击到显示界面 |
| **内存占用** | < 200MB | 正常运行状态 |
| **最大并发** | 32 玩家 | 服务端支持 |

### 3.2 可靠性需求

| 指标 | 要求 |
|------|------|
| **崩溃恢复** | 自动记录崩溃日志 |
| **断线容忍** | 10 秒内可重连恢复 |
| **数据一致性** | 服务端为唯一数据源 |

### 3.3 兼容性需求

| 平台 | 版本 |
|------|------|
| **操作系统** | Windows 10/11 |
| **Python** | 3.9+ |
| **依赖库** | PyQt6, websockets, psutil |

### 3.4 部署需求

| 方式 | 说明 |
|------|------|
| **打包格式** | PyInstaller 单文件 exe |
| **服务端端口** | 8888 (TCP), 23333 (UDP) |
| **防火墙** | 需开放入站规则 |

---

## 四、接口规格

### 4.1 消息类型常量

详见 `protocol.md` 文档。

### 4.2 错误码定义

| 错误码 | 说明 | 责任方 |
|--------|------|--------|
| `NAME_TAKEN` | 名字已被占用 | 服务端 |
| `ROOM_FULL` | 房间已满 | 服务端 |
| `NOT_ADMIN` | 非管理员无权操作 | 服务端 |
| `INVALID_MESSAGE` | 消息格式错误 | 服务端 |
| `SERVER_ERROR` | 服务器内部错误 | 服务端 |
| `RECONNECT_FAILED` | 重连失败（数据过期） | 服务端 |
| `NETWORK_TIMEOUT` | 网络超时 | 服务端 |
| `NOT_READY` | 有玩家未准备 | 服务端 |
| `ALREADY_PAUSED` | 已处于暂停状态 | 服务端 |

---

## 五、验收标准

### 5.1 核心功能验收

| 用例 ID | 验收条件 | 测试方法 |
|---------|---------|---------|
| **AC-001** | 客户端能通过 UDP 自动发现局域网服务器 | 启动服务器，启动客户端，观察是否自动连接 |
| **AC-002** | 支持手动输入 IP 连接 | 禁用网络，启动客户端手动输入 IP，能成功连接 |
| **AC-003** | 支持 2-32 名玩家同时在线 | 创建 32 个客户端实例，验证都能加入 |
| **AC-004** | 管理员能开始比赛 | 所有玩家准备后，管理员点击开始，观察倒计时 |
| **AC-005** | 打字正确/错误能正确判定 | 输入正确字符显示绿色，错误字符显示红色下划线 |
| **AC-006** | 暂停/恢复功能正常 | 管理员暂停，观察客户端显示"比赛暂停"；恢复后正常继续 |
| **AC-007** | 断线后 10 秒内重连能恢复 | 断开网络 5 秒后重连，验证状态恢复 |
| **AC-008** | 比赛结果正确计算 | 完成打字后，验证得分计算公式正确 |

### 5.2 性能验收

| 用例 ID | 验收条件 | 达标标准 |
|---------|---------|---------|
| **AC-101** | 网络延迟测试 | keystroke_ack 响应 < 100ms |
| **AC-102** | UI 流畅度 | 无明显卡顿，60fps |
| **AC-103** | 内存占用 | 客户端 < 150MB，服务端 < 200MB |

### 5.3 异常处理验收

| 用例 ID | 验收条件 | 预期行为 |
|---------|---------|---------|
| **AC-201** | 输入重复名字 | 显示"名字已被占用"错误 |
| **AC-202** | 非管理员尝试暂停 | 显示"无权限"错误 |
| **AC-203** | 服务器崩溃 | 客户端显示断开连接，保存状态 |

---

## 六、需求跟踪矩阵

| 需求 ID | 需求描述 | 优先级 | 状态 | 版本 | 备注 |
|---------|---------|--------|------|------|------|
| FR-001 | UDP 服务发现 | P0 | 已实现 | v1.0 | |
| FR-002 | WebSocket 连接 | P0 | 已实现 | v1.0 | |
| FR-003 | 手动 IP 连接 | P1 | 已实现 | v1.0 | |
| FR-010 | 加入房间 | P0 | 已实现 | v1.0 | |
| FR-011 | 玩家列表 | P0 | 已实现 | v1.0 | |
| FR-012 | 准备状态 | P1 | 已实现 | v1.0 | |
| FR-020 | 赛前倒计时 | P0 | 已实现 | v1.0 | |
| FR-021 | 比赛开始 | P0 | 已实现 | v1.0 | |
| FR-022 | 逐字校验 | P0 | 已实现 | v1.0 | |
| FR-023 | 时间同步 | P0 | 已实现 | v1.0 | |
| FR-024 | 进度上报 | P1 | 已实现 | v1.0 | |
| FR-030 | 暂停比赛 | P1 | 已实现 | v1.0 | |
| FR-031 | 恢复比赛 | P1 | 已实现 | v1.0 | |
| FR-032 | 强制结束 | P2 | 已实现 | v1.0 | |
| FR-040 | 轮次结束 | P0 | 已实现 | v1.0 | |
| FR-041 | 最终排名 | P0 | 已实现 | v1.0 | |
| FR-050 | 得分计算 | P0 | 已实现 | v1.0 | |
| FR-060 | 断线检测 | P1 | 已实现 | v1.0 | |
| FR-061 | 状态保留 | P1 | 已实现 | v1.0 | |
| FR-062 | 重连恢复 | P1 | 已实现 | v1.0 | |
| FR-070 | UI 页面 | P0 | 已实现 | v1.0 | |
| FR-071 | 快捷键 | P2 | 已实现 | v1.0 | F11 全屏 |
| FR-072 | 字体自适应 | P2 | 已实现 | v1.0 | |

---

## 七、附录

### A. 参考文档

| 文档 | 路径 |
|------|------|
| 通信协议 | `server/protocol.md` |
| 打包说明 | `打包说明.md` |
| 连接问题解决 | `局域网连接问题解决指南.md` |

### B. 技术栈

| 组件 | 技术选型 | 版本 |
|------|---------|------|
| GUI 框架 | PyQt6 | 6.x |
| 网络通信 | websockets | 12.x |
| 进程管理 | psutil | 5.x |
| 打包工具 | PyInstaller | 6.x |
| Python 版本 | Python | 3.9+ |

---

*文档版本历史*
| 版本 | 日期 | 修改内容 |
|------|------|---------|
| v1.0 | 2026-05-12 | 初始版本 |
